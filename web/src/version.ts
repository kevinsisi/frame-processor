// v0.2.0 — bundled processing pipeline: NAFNet denoise + lens distortion correction +
// Gemini Vision level correct + YOLOv8 auto crop + Pillow color grade + before/after slider.
export const APP_VERSION = "0.2.0";
